import styled from '@emotion/styled';

const PageNavigationHover = styled.div`
  position: absolute;
  cursor: grabbing;
  &:hover {
    background: rgba(220, 220, 220, 0.2);
  }
`;

export default PageNavigationHover;
